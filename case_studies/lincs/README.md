# LINCS Case Study

This case checks that the LINCS-style example surfaces sparse/absent control support, keeps the
metadata-only shortcut probe low, and reports target-effect insufficiency for expression values
that are not suitable for target log-fold-change checks.

Dataset expected at:

```text
data/real/lincs_example.h5ad
```

Config:

```text
configs/lincs_example.yaml
```
