# SciPlex Case Study

This case checks that the SciPlex-style example surfaces sparse/absent control support and
insufficient direct target-effect metadata while keeping the metadata-only shortcut probe low.

Dataset expected at:

```text
data/real/sciplex_example.h5ad
```

Config:

```text
configs/sciplex_example.yaml
```
