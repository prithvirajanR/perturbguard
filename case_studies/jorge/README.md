# Jorge Case Study

This case checks that the Jorge-style variant perturbation example surfaces complete
perturbation-batch concentration and a strong metadata-only shortcut, while direct gene target
effects remain insufficient because variant labels do not map to measured genes.

Dataset expected at:

```text
data/real/Jorge_example.h5ad
```

Config:

```text
configs/jorge_example.yaml
```
